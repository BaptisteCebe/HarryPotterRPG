package com.isep.harrypotterrpg.Character.wizard.wand;
import java.util.Random;
import java.util.Scanner;

/*
        public void randomWand() {
            Random random = new Random();
            Scanner scanner = new Scanner(System.in);

            String[] coeurs = {"plume de phénix", "crin de licorne", "ventricule de dragon", "cheveu de Vélane", "écaille de sirène"};
            int coeurAleatoire = random.nextInt(coeurs.length);

            System.out.print("Entrez la taille de votre baguette magique : ");
            int taille = scanner.nextInt();

            System.out.println("Votre baguette magique mesure " + taille + " cm et a un coeur en " + coeurs[coeurAleatoire] + ".");

        }
    */

